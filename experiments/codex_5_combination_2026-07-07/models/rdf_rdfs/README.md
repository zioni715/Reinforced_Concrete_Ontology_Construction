# RDF/RDFS Baseline Model

This baseline combination intentionally has no extra extension file.

The model consists of:

- `ontology/ontology.ttl`
- `ontology/sample_data.ttl`

It is used as the control model for the 5-combination Codex evaluation.
